@model IEnumerable<FAQServiceModel>

@{
    var j = 0;
}


@if (!Model.Any())
{
    <div class="jumbotron">
        <h1 class="display-4">Information</h1>
        <p class="lead">There are no FAQ Information currently.Please contact support for assistance.</p>
    </div>

}
else
{
    <div class="row">
        @foreach (var faq in Model)
        {
            <div class="col-md-4 div-planes-partial">
                <div class="card mb-3 planes-partial">
                    <img class="card-img-top img-planes-partial" src="@faq.ImageUrl">
                    <div class="accordion" id="accordionExample">
                        <div class="card-header" id="heading_@j">
                            <h2 class="mb-0">
                                <button class="btn btn-link btn-block text-sm-center" type="button" data-toggle="collapse" data-target="#collapse_@j" aria-expanded="true" aria-controls="collapse_@j">
                                    @faq.Title
                                </button>
                                @if (User.IsAdmin())
                                {
                                    <a asp-area="Admin" asp-controller="FAQ" asp-action="Edit" asp-route-id="@faq.Id" class="btn btn-warning">Edit</a>
                                    <a asp-area="Admin" asp-controller="FAQ" asp-action="ChangeVisibility" asp-route-id="@faq.Id" class="btn btn-info float-right ml-3">@(faq.IsPublic ? "Hide" : "Show")</a>
                                }
                            </h2>
                        </div>

                        <div id="collapse_@j" class="collapse" aria-labelledby="heading_@j" data-parent="#accordionExample">
                            <div class="card-body">
                                @faq.Description
                            </div>
                        </div>
                    </div>
                </div>

            </div>
            j++;
        }
    </div>

















    @*    <div class="row">
    @foreach (var faq in Model)
    {
    <div class="card" style="width: 18rem;">
    <img class="card-img-top img-planes-partial" src="@faq.ImageUrl">


    <p>
    <button class="btn btn-primary" type="button" data-toggle="collapse" data-target="#collapseWidthExample" aria-expanded="false" aria-controls="collapseWidthExample">
    @faq.Title
    </button>
    </p>
    <div style="min-height: 120px;">
    <div class="collapse width" id="collapseWidthExample">
    <div class="card card-body" style="width: 320px;">
    <p class="card-text">@faq.Description</p>
    </div>
    @if (User.IsAdmin())
    {
    <a asp-area="Admin" asp-controller="FAQ" asp-action="Edit" asp-route-id="@faq.Id" class="btn btn-warning">Edit</a>
    <a asp-area="Admin" asp-controller="FAQ" asp-action="ChangeVisibility" asp-route-id="@faq.Id" class="btn btn-info float-right ml-3">@(faq.IsPublic ? "Hide" : "Show")</a>
    }
    </div>
    </div>
    </div>
    }
    </div>*@
}

